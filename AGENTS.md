# 会话协作规则

所有在此仓库工作的 AI 会话必须遵守：

## 提交纪律

- **不要自动 commit / push。** 只修改文件、运行构建验证（`npm run docs:build`）；除非用户明确要求提交或推送。
- 用户要求提交时，**只 `git add` 用户指定的文件**，禁止 `git add -A` / `git add .`——可能有其他会话的未完成改动在工作区。
- 可能存在多个会话并行编辑本仓库；动手前先 `git status` / `git diff` 了解现状，不要回滚不属于本次任务的改动。

## 脱敏纪律（公开仓库）

- 本仓库是公开站点，**不得引入**：内部仓库名、内部包名（如 npm scope）、公司/团队标识、业务模块实名、本地绝对路径（`/Users/...`）。
- 涉及内部实践的段落一律用通用描述（如"工具集""某内部仓库"），不写真实名称。
- `REVIEW.md` 是内部文档，已在 `.gitignore` 中，永远不要提交它。

## Git 身份

- 本仓库已配置 repo 级 `user.name` / `user.email`（GitHub noreply 格式），**不要改回**真实姓名或公司邮箱。
- 不要重写已推送的历史，除非用户明确要求。

## 写作与查证

- **写作姿态**：文章要有逻辑，上下衔接自然；减少黑话和抽象概念，以一个朴实的工程师的身份去写——先讲问题，再讲方案，最后讲代价；用前端工程师熟悉的概念做类比，不堆术语。
- **机制优先，概念最少**：多讲机制和原理（数据怎么流、部件怎么协作、失败时会发生什么），少造抽象名词；一个概念只有对理解机制必要时才引入，且引入时必须立刻给出它解决的具体问题——读者说"看不懂"时，缺的通常是机制和场景，不是更多术语。
- **每个环节回答四问**：它为什么存在（不解决会怎样）、为什么需要做、怎么做的、优缺点和代价是什么。写完一节要自查：读者能否回答这四问；答不上，就补问题场景或机制细节，而不是补形容词和总结句。
- **多讲问题和挑战**：先让读者撞上真实的坑（给出会翻车的具体场景），再引出方案；方案的合理性由问题撑起，不由"业界都这么做"撑起。
- **不引源码**：正文不要携带源码片段或源码文件路径（如 `packages/.../xx.ts`、`codex-rs/.../xx.rs`）；本册的定位是讲清楚原理与实现取舍，不是源码解析。需要示意时用简化的伪代码或文字描述；仓库级的精读入口统一放在附录 B，不在各章重复。
- **查证优先级**：查资料、回答疑问时，优先以本册引用的三个 Agent 仓库和 ai-agent-book 为主：`codex`（OpenAI）、`grok-build`（SpaceXAI）、`deepseek-harness`/dsh（DeepSeek）、`ai-agent-book`（bojieli）。本地克隆为同级目录 `../codex`、`../grok-build`、`../deepseek-harness`、`../ai-agent-book`；结论尽量落到源码或书稿的具体位置，拿不准就明确标注不确定，不要编造。

